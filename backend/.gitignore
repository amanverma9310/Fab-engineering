node_modules
.env
.env.local
*.log
npm-debug.log*
.DS_Store

# Local dev file uploads — not needed once Cloudinary is configured, and
# should never be committed either way.
uploads/**/*
!uploads/**/.gitkeep
